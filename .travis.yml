language: go
git:
  depth: 1
install:
  - uname -a
  - wget https://github.com/gohugoio/hugo/releases/download/v0.60.1/hugo_0.60.1_Linux-64bit.deb
  - sudo dpkg -i hugo*.deb
  - hugo version
  - ls
  - pwd
script:
  ‐ hugo
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  on:
    branch: source
  local_dir: public
  repo: yufulou/yufulou.github.io
  fqdn: yufulou.github.io
  target_branch: master
  email: yufulou@qq.com
  name: yufulou
